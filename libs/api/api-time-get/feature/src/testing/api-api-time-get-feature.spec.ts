import { apiApiTimeGetFeature } from './api-api-time-get-feature';

describe('apiApiTimeGetFeature', () => {
  it('should work', () => {
    expect(apiApiTimeGetFeature()).toEqual('api-api-time-get-feature');
  });
});
