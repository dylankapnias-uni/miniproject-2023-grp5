import { apiApiTimeGetUtil } from './api-api-time-get-util';

describe('apiApiTimeGetUtil', () => {
  it('should work', () => {
    expect(apiApiTimeGetUtil()).toEqual('api-api-time-get-util');
  });
});
