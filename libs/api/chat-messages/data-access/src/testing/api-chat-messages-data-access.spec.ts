import { apiChatMessagesDataAccess } from './api-chat-messages-data-access';

describe('apiChatMessagesDataAccess', () => {
  it('should work', () => {
    expect(apiChatMessagesDataAccess()).toEqual(
      'api-chat-messages-data-access'
    );
  });
});
