import { apiChatMessagesFeature } from './api-chat-messages-feature';

describe('apiChatMessagesFeature', () => {
  it('should work', () => {
    expect(apiChatMessagesFeature()).toEqual('api-chat-messages-feature');
  });
});
