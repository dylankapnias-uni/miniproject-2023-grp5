import { apiChatMessagesUtil } from './api-chat-messages-util';

describe('apiChatMessagesUtil', () => {
  it('should work', () => {
    expect(apiChatMessagesUtil()).toEqual('api-chat-messages-util');
  });
});
