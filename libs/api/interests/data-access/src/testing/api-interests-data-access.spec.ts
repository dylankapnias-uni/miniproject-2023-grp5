import { apiInterestsDataAccess } from './api-interests-data-access';

describe('apiInterestsDataAccess', () => {
  it('should work', () => {
    expect(apiInterestsDataAccess()).toEqual('api-interests-data-access');
  });
});
