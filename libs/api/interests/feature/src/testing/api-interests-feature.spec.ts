import { apiInterestsFeature } from './api-interests-feature';

describe('apiInterestsFeature', () => {
  it('should work', () => {
    expect(apiInterestsFeature()).toEqual('api-interests-feature');
  });
});
