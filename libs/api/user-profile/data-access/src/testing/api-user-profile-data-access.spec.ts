import { apiUserProfileDataAccess } from './api-user-profile-data-access';

describe('apiUserProfileDataAccess', () => {
  it('should work', () => {
    expect(apiUserProfileDataAccess()).toEqual('api-user-profile-data-access');
  });
});
