import { apiUserProfileFeature } from './api-user-profile-feature';

describe('apiUserProfileFeature', () => {
  it('should work', () => {
    expect(apiUserProfileFeature()).toEqual('api-user-profile-feature');
  });
});
