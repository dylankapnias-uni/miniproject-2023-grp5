import { apiUserProfileUtil } from './api-user-profile-util';

describe('apiUserProfileUtil', () => {
  it('should work', () => {
    expect(apiUserProfileUtil()).toEqual('api-user-profile-util');
  });
});
